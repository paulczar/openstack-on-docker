[template]
src   = "reload"
dest  = "/app/bin/reload"
owner = "root"
group = "root"
mode  = "0755"
keys = [
  "/openstack/database/credentials",
]
check_cmd = "/app/bin/check {{ .src }}"
reload_cmd = "/app/bin/reload"
