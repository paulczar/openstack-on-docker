[template]
src   = "utf8.cnf"
dest  = "/etc/mysql/conf.d/utf8.cnf"
owner = "root"
group = "root"
mode  = "0644"
keys = [
  "/openstack/database",
]
check_cmd = "/usr/sbin/mysqld --help > /dev/null"
reload_cmd = "kill -1 `cat /app/database.pid`"
