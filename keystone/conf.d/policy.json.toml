[template]
src   = "policy.json"
dest  = "/etc/keystone/policy.json"
owner = "root"
group = "root"
mode  = "0644"
keys = [
  "/openstack/keystone",
  "/openstack/database",
]
check_cmd = "/app/bin/check {{ .src }}"
