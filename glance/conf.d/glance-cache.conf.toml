[template]
src   = "glance-cache.conf"
dest  = "/etc/glance/glance-cache.conf"
owner = "root"
group = "root"
mode  = "0644"
keys = [
  "/openstack/keystone",
  "/openstack/glance",
  "/openstack/database",
]
check_cmd = "/app/bin/check {{ .src }}"
